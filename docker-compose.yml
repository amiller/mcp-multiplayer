services:
  mcp-multiplayer:
    build: .
    ports:
      - "8100:8100"
      - "8201:8201"
    environment:
      - DOMAIN=${DOMAIN:-localhost}
      - USE_SSL=false
      - PROXY_HOST=0.0.0.0
      - PROXY_PORT=8100
      - MCP_HOST=0.0.0.0
      - MCP_PORT=8201